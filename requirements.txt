# bREadbeats - Audio Beat Detection to Restim
# Core runtime dependencies

# GUI Framework
PyQt6>=6.5.0

# Audio Processing & Math
numpy>=1.24.0                    # Core DSP/math used across the app
sounddevice>=0.4.6             # Audio input capture
scipy>=1.10.0                  # Butterworth bandpass filter for beat detection
pyaudiowpatch>=0.2.12; platform_system == "Windows"   # WASAPI loopback capture

# Visualization & Display
pyqtgraph>=0.13.0              # Real-time audio/beat visualization

# Windows Audio Support
comtypes>=1.2.0; platform_system == "Windows"         # Windows COM interface for WASAPI
pycaw>=20230407; platform_system == "Windows"         # Windows volume sensing (enabled by default; pulls psutil transitively)
